# Local API base URL — set to your machine's LAN IP when testing on a physical device
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.100:5001
# Leave empty to use localhost (works in simulators/emulators)
EXPO_PUBLIC_API_URL=http://localhost:5001
